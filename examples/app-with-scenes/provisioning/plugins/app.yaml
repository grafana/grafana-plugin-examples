apiVersion: 1

apps:
  - type: myorg-scenes-app
    org_id: 1
    org_name: Main Org.
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
