apiVersion: 1

datasources:
  - name: Http Backend Data Source
    type: grafana-datasourcehttpbackend-datasource
    access: proxy
    isDefault: true
    orgId: 1
    version: 1
    editable: true
    jsonData:
      path: '/resources'
      defaultTimeField: 
    secureJsonData: 
      apiKey: 'api-key'
