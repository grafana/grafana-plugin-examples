apiVersion: 1

datasources:
  - name: Streaming websocket datasource
    type: example-websocket-datasource
    access: proxy
    isDefault: true
    orgId: 1
    version: 1
    editable: true
    jsonData:
      path: '/resources'
      defaultTimeField: 
    secureJsonData: 
      apiKey: 'api-key'
